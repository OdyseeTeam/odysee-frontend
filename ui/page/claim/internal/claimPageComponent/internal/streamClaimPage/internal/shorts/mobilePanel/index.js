// @flow
import { connect } from 'react-redux';
import MobileCommentsModal from './view';

const select = (state, props) => ({});

const perform = (dispatch, ownProps) => ({});

export default connect(select, perform)(MobileCommentsModal);
