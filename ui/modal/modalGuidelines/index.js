import { connect } from 'react-redux';
import modalGuidelines from './view';

const select = () => ({});

const perform = (dispatch) => ({});

export default connect(select, perform)(modalGuidelines);
