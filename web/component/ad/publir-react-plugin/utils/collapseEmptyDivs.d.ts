import { Config } from '../models/config';
export declare const getInitialAdSlotSize: (config: Config, id: string) => {
    width: number;
    height: number;
};
export declare const getEmptyAdSlotSize: (config: Config, id: string) => {
    width: number;
    height: number;
};
